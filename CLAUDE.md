# walleve 運用ルール

## 🚫 Google Drive は使わない（最重要）

このプロジェクトでは **Google Drive を一切使わない**。理由：容量が逼迫しており、
過去に自動化が下書き・メモ・ログ・認証情報を Drive に大量生成して重複が溜まったため。

Claude（自動化）は次を厳守すること：

- **Google Drive への書き込み・コピー・新規作成をしない**
  （`create_file` / `copy_file` などの Drive 書き込み系ツールを呼ばない）
- **Google Drive の読み取りもしない**（必要な情報はこのリポジトリ内に置く）
- 作業ログ・下書き・メモ・方向性メモの類は **すべてこのリポジトリに保存**する
- **認証情報（パスワード・アプリケーションパスワード等）を Drive に置かない**

## 保存先のルール（Drive の代わり）

すべてこの Git リポジトリに保存する。目安のフォルダ構成：

| 種類 | 保存先 |
|---|---|
| 記事の下書き（Markdown/HTML） | `articles/` |
| 投稿ログ・活動報告 | `logs/` |
| ネタ出し・方向性メモ・決定事項 | `memo/` |

ファイル名は日付を含める（例：`logs/2026-07-27_投稿ログ.md`）。
**同じ内容を毎回新規作成せず、既存ファイルを上書き更新する**（重複を作らない）。

## 記事自動投稿ワークフロー

- 週次クリーニング記事：スキル `weekly-article-senntoku` を使用。
  WordPress REST API に直接下書き投稿するため、中間ファイルは不要。
- わんこ／夢の街／ワンティル系の記事作成：下書きは `articles/` に置き、
  仕上げ後に各投稿先（WordPress・はてな等）へ展開する。Drive は経由しない。

## 対象サイト / プロジェクト

- わんこのハッピーライフ
- 夢の街グループ
- ワンティル
- 洗濯ソムリエが厳選（https://senntoku.walleve.net/）
